**Database query caching proxy**

1. Let's build a database query caching system. 
2. The interface defines a simple query operation
3. The real implementation simulates a slow database
4. Caching proxy stores results and returns cached values for repeated queries
